type CardCompactProps = {
  title: string;
  description?: string;
  className?: string;
  content: React.ReactNode;
  footer?: React.ReactNode;
}

export function CardCompact({ title, description, className, content, footer }: CardCompactProps) {
  return (
  );
}
